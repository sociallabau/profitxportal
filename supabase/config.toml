project_id = "yzpjoxtqpqyzgsptplzr"
# Called by Deepgram, which cannot present a Supabase JWT. Both verify their
# own one-time token instead.
[functions.transcript-media]
verify_jwt = false

[functions.transcribe-callback]
verify_jwt = false

# Called as the src of an <img>, which cannot present a JWT. Restricted to
# public Instagram CDN hosts in code instead.
[functions.proxy-image]
verify_jwt = false

# Public calendar feed, read by the portal on load. No user data involved.
[functions.calendar-feed]
verify_jwt = false
